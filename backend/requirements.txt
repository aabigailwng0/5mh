# --- Core engine (always required) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-multipart==0.0.9      # multipart form / file uploads
Pillow==10.3.0               # image decoding
numpy==1.26.4                # feature math
opencv-python-headless==4.10.0.84  # color-space + texture features (no GUI deps)
pandas==2.2.2                # Sephora catalog + daily-log tables
requests==2.32.3             # Open Beauty Facts client

# --- Attribution stretch goal (distributed-lag regression + attribution) ---
scikit-learn==1.5.0
statsmodels==0.14.2

# --- OPTIONAL: pretrained CNN backend (dermafyr MobileNetV2 / ACNE04). ---
# The engine runs fully without this; install only if you want to plug in the
# shipped Keras model as a refinement signal for the acne axis.
# tensorflow==2.16.1

# --- OPTIONAL: LLM vision scorer (score the four axes with Claude or Gemini). ---
# Install the SDK for whichever provider you set via SKINALIZER_LLM_PROVIDER.
# Without these (or without an API key) the engine stays on the CV scorer.
# anthropic==0.34.2              # SKINALIZER_LLM_PROVIDER=anthropic  (+ ANTHROPIC_API_KEY)
# google-generativeai==0.7.2     # SKINALIZER_LLM_PROVIDER=gemini     (+ GEMINI_API_KEY)
